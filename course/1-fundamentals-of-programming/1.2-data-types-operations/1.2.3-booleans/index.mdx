import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="1. Fundamentals of Programming/1.2 Data Types & Operations/1.2.3 Booleans/1.2.3.1 Introduction" />

# 1.2.3 Booleans - Introduction

## Quick concept map

- Boolean values: True and False (and None for an empty value).
- Comparison blocks compare values and produce booleans.
- Logical blocks combine boolean results with `and` / `or`.
- Unary blocks negate or check identity (`not`, `is None`, `is not None`).
- The conditional (ternary) block chooses between two values depending on a condition.

## 1) Comparison block (pill with dropdown)

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.3-booleans/res/BOO-1.1.png"
  alt="Comparison block: left op right"
  style={{ width: "50%" }}
/>

Python equivalents and examples:

```python
# equality and inequality
print(3 == 4)   # False
print(3 != 4)   # True

# comparisons
print(3 < 4)    # True
print(5 >= 5)   # True
```

Note: Python supports chained comparisons. The block editor offers a three-input comparison pill too:

Boilerplate for chained example:

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.3-booleans/res/BOO-1.2.png"
  alt="Chained comparison block: a <= b <= c"
  style={{ width: "50%" }}
/>

```python
print(3 <= 4 <= 5)  # True (3 <= 4 and 4 <= 5)
```

## 2) Conditional expression block (ternary)

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.3-booleans/res/BOO-1.3.png"
  alt="Ternary conditional block: value_if_true if condition else value_if_false"
  style={{ width: "50%" }}
/>

This block maps directly to Python's `x if cond else y` expression. It's handy for short choices.

```python
print(1 if True else 0)         # prints 1
print(100 if 3 < 2 else -100)   # prints -100

# nested ternary (read carefully):
print(100 if True else (-100 if True else 0))
```

Tip: keep nested ternaries short; prefer normal `if` statements for complex logic.

## 3) Logical operator block (`and` / `or`)

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.3-booleans/res/BOO-1.4.png"
  alt="Logical operator block: left and/or right"
  style={{ width: "50%" }}
/>

Python examples:

```python
print(True and False)   # False
print(True or False)    # True

# combine comparisons
print((3 < 4) and (5 > 2))  # True
```

Small note on short-circuiting: `and` and `or` return the first value that decides the result in Python — this matters when their operands are non-boolean values.

## 4) Unary boolean block (`not`, `is none`, `is not none`)

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.3-booleans/res/BOO-1.5.png"
  alt="Unary boolean block: not / is none / is not none"
  style={{ width: "50%" }}
/>

Examples:

```python
print(not True)        # False

value = None
print(value is None)   # True
print(value is not None) # False
```

`is None` checks identity against `None` (recommended for checking emptiness).

## 5) Boolean & None literals

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.3-booleans/res/BOO-1.6.png"
  alt="Boolean and None literals: true, false, none"
  style={{ width: "25%" }}
/>

```python
print(True)
print(False)
print(None)
```

## Quick exercises (preview)

- Try writing a block that compares two numbers and then uses the ternary to return 1 or 0. Convert it to Python and run it.
- Combine comparisons with `and`/`or` and predict the result before running.
- Use `is None` to check if a variable is empty, then use a ternary to provide a default number.

## Notes about Pybricks block limitations

- Some expressions that are easy to type in Python (like `a + b * 2`) may need to be expressed with nested blocks in the editor. When in doubt, group sub-expressions in parentheses or use nested pills.
- The examples above use only variables, numbers, booleans, `None`, and the operators supported by the editor.

If this matches your expectations I will:

- Create the Tasks (3 difficulties, implement + predict as in the previous chapter) using only the allowed blocks, and
- Produce a `solutions.mdx` file with image boilerplate and step-by-step derivations.

Tell me to proceed and I'll start the Tasks next.

import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="1. Fundamentals of Programming/1.2 Data Types & Operations/1.2.4 Booleans/1.2.4.2 Tasks" />

# 1.2.4.2 Booleans - Tasks

Below are practice tasks based only on the material covered so far: variables, numbers, strings, and booleans (comparisons, logical operations, ternary operators, and None). We'll provide a separate solutions article later — try these on your own first.

Rules:

- The tasks can be solved with the things you have learned so far.
- You can use everything from the Variables, Numbers, Strings, and Booleans sections
- But you cannot use anything else from other sections (like control flow, loops, etc.)

---

## Easy

### Implement

#### 1. Print basic boolean values

Goal: Display the two fundamental boolean values on separate lines.

Expected output:

```
True
False
```

#### 2. Compare two numbers

Goal: Store the numbers 15 and 20 in variables, then check if the first number is greater than the second.

Expected output:

```
False
```

#### 3. Test equality

Goal: Create two variables with the same value (your choice of number) and verify they are equal.

Expected output:

```
True
```

### Predict

#### 4. Multiple comparisons

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-1.1.png"
  alt="Multiple comparisons prediction"
  style={{ width: "50%" }}
/>

```python
x = 10
y = 20
print(x < y)
print(x == y)
print(x >= y)
```

#### 5. Logical operations

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-1.2.png"
  alt="Logical operations prediction"
  style={{ width: "50%" }}
/>

```python
print(True and False)
print(False or True)
print(not True)
print(not False)
```

#### 6. Chain comparison result

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-1.3.png"
  alt="Chain comparison result prediction"
  style={{ width: "50%" }}
/>

```python
temperature = 25
print(20 <= temperature <= 30)
print(10 < temperature < 20)
print(temperature == 25)
```

---

## Medium

### Implement

#### 7. Battery check simulation

Goal: Create a battery level variable (set it to 35) and check if it's both greater than 30 AND less than 80.

Expected output:

```
True
```

#### 8. Robot command validation

Goal: Create a command variable (set to "move forward") and check if it contains the word "move" AND doesn't contain the word "stop".

Expected output:

```
True
```

#### 9. Speed selection

Goal: Create a battery level variable (set to 25). Use a ternary operator to choose "Slow" if battery is below 50, otherwise choose "Fast".

Expected output:

```
Slow
```

### Predict

#### 10. Complex logical expression

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-2.1.png"
  alt="Complex logical expression prediction"
  style={{ width: "75%" }}
/>

```python
a = 15
b = 25
c = 20
print((a < b) and (b > c))
print((a == 15) or (c < 10))
print(not (a > b))
```

#### 11. Chained comparisons with variables

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-2.2.png"
  alt="Chained comparisons with variables prediction"
  style={{ width: "75%" }}
/>

```python
min_val = 5
sensor = 12
max_val = 20
print(min_val < sensor < max_val)
print(0 <= sensor <= 10)
```

#### 12. String operations with booleans

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-2.3.png"
  alt="String operations with booleans prediction"
  style={{ width: "75%" }}
/>

```python
robot_status = "battery low"
error_msg = "sensor error"
print("battery" in robot_status)
print("low" in robot_status and "error" not in robot_status)
print("sensor" in error_msg or "battery" in error_msg)
```

---

## Hard

### Implement

#### 13. Multi-condition robot status

Goal: Create variables for battery (45), temperature (28), and a boolean for daylight (True). Check if the robot is ready to operate: battery above 40, temperature below 30, and it's daytime.

Expected output:

```
True
```

#### 14. Nested ternary decision

Goal: Create a score variable (set to 75). Use nested ternary operators to assign a grade: "A" if score >= 90, "B" if score >= 80, otherwise "C".

Expected output:

```
C
```

#### 15. None value checking

Goal: Create a variable set to None, then check if it is None and if it is not None on separate lines.

Expected output:

```
True
False
```

### Predict

#### 16. Advanced logical combinations

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-3.1.png"
  alt="Advanced logical combinations prediction"
  style={{ width: "100%" }}
/>

```python
x = 30
y = 40
z = 35
print((x < z < y) and (z % 5 == 0))
print((x > 25) or (y < 35) and (z != 35))
print(not ((x < y) and (y > z)))
```

#### 17. Ternary with comparisons

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-3.2.png"
  alt="Ternary with comparisons prediction"
  style={{ width: "100%" }}
/>

```python
speed = 60
limit = 50
result = "OK" if speed <= limit else ("Warning" if speed <= limit + 10 else "Danger")
print(result)

battery = 85
status = "High" if battery > 75 else ("Medium" if battery > 25 else "Low")
print(status)
```

#### 18. Complex range validation logic

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-3.3.png"
  alt="Complex range validation logic prediction"
  style={{ width: "100%" }}
/>

```python
value = 42
range1_valid = 30 <= value <= 50
range2_valid = 70 <= value <= 90
print(range1_valid)
print(range2_valid)
print(range1_valid or range2_valid)
print(range1_valid and range2_valid)
```

---

## Challenge

### Implement

#### 19. Robot safety system

Goal: Create a comprehensive safety check with variables: battery (30), obstacles_detected (False), emergency_stop (False), and system_ok (True). The robot is safe to operate if: battery > 25, no obstacles detected, no emergency stop, and system is ok.

Expected output:

```
True
```

#### 20. Sensor fusion decision

Goal: Create three sensor readings: sensor1 (12), sensor2 (8), sensor3 (15). The system should return True if at least 2 out of 3 sensors read values between 10 and 20.

Expected output:

```
True
```

#### 21. Smart battery management

Goal: Create variables for current_battery (60), charging (False), and power_saving (True). The system should activate if: battery is below 70 AND either charging is active OR power saving is enabled.

Expected output:

```
True
```

### Predict

#### 22. Ultimate boolean challenge

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-4.1.png"
  alt="Ultimate boolean challenge prediction"
  style={{ width: "100%" }}
/>

```python
a, b, c = 10, 20, 15
condition1 = (a < b < c * 2) and (c % 5 == 0)
condition2 = (b >= 20) or (a == c - 5)
condition3 = not (a > c)
final_result = condition1 and condition2 and condition3
print(condition1)
print(condition2)
print(condition3)
print(final_result)
```

#### 23. Complex ternary chains

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-4.2.png"
  alt="Complex ternary chains prediction"
  style={{ width: "100%" }}
/>

```python
score = 65
grade = "A" if score >= 90 else ("B" if score >= 80 else ("C" if score >= 70 else "D"))
print(grade)

level = 45
difficulty = "Easy" if level < 25 else ("Medium" if level < 75 else "Hard")
print(difficulty)
```

#### 24. Boolean operator precedence

What will this code print?

<img
  src="/1-fundamentals-of-programming/1.2-data-types-operations/1.2.4-booleans/res/PRED-4.3.png"
  alt="Boolean operator precedence prediction"
  style={{ width: "100%" }}
/>

```python
x = True
y = False
z = True
print(x or y and z)
print((x or y) and z)
print(x and not y or z)
print(not x or y and z)
```
